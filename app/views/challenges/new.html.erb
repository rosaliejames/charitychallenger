Create a New Challenge
<%= form_for @challenge do |f| %>
	Pick a User to Challenge:
  <%= f.select( :challengee_id, @users) %> <br />

  <%= f.label "Enter a challenge description" %>:
  <%= f.text_field :challenge_description %><br />

  <%= f.label "Number of days" %>:
  <%= f.text_field :total_days %><br />

  Pick a Charity:
  <%= f.select( :challenger_charity_id, @charities) %> <br />

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
            data-key="<%= ENV['STRIPE_PUBLIC_KEY'] %>"
            data-description="<%= "Getting closer to your goal!" %>"
            data-amount="<%= (100).to_i %>"></script>

  <%= f.submit %>
   <% end %>

  
  